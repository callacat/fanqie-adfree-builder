# 仓库收尾清理指令（2026-09-12，东哥拍板）

> 背景：破解版净化线 v9 已交付+真机四判据全过（任务 recvul3UNi4ptV done）。仓库进入维护态，按东哥指令清理多余文件、更新文档、整理 Release 与 Actions 记录。
> 分工：**git 树清理+文档更新=码农执行**（本文档 §1-§2）；**Release 删除+Action runs 删除=老马 API 执行**（§3，码农勿动）。

## 0. 清理原则（红线）

- **samples Release 禁删**：crack-patch.yml / crack-diff.yml 运行时 `gh release download samples` 拉 mod 包+inner 原包——删了整条 CI 线断
- **所有 tag 保留**（存储成本≈0，报告引用锚点）；只删 Release（=资产+说明文字）
- **v9 Release 保留**（最终交付物 + 真机验收版本）
- diff-baksmali 可再生：Actions → fanqie-crack-diff 重跑即可（samples 底包在），不留仓

## 1. git 树清理（码农执行）

```bash
cd /home/agent/workspace/fanqie-builder && git pull
# 1a) 移出 303k 文件 diff 归档（381MB 仓库体积的 99%，可再生，见 §0）
git rm -r --quiet docs/diff-baksmali/
# 1b) 删搁置路线 workflow（保留 crack-diff.yml + crack-patch.yml 两条现役）
git rm .github/workflows/build.yml .github/workflows/v6-lspatch.yml .github/workflows/b3-shell.yml
# 1c) 删搁置路线专属脚本（现役保留：crack_diff.py, patch_16b.py, extract_cert.py, gen_builtin_cert.py）
git rm scripts/patch_fanqie.py scripts/patch_illegal.py scripts/patch_stub.py scripts/b3-shell-build.sh
# ⚠️ 删除前逐个 grep 确认现役 yml 无引用：git grep patch_fanqie -- .github/
# 1d) .gitignore 追加 docs/diff-baksmali/（防误重提交）
```

## 2. 文档更新（码农执行）

- **README.md 重写**到终态：当前唯一路线=破解版净化改造（方案-破解版改造.md）；版本谱系一句话（16a diff→16b 卡密链 P1-P3→17 P4/P5 崩溃修复→17b P7→v9 真机四判据全过）；docs 索引表按现存文件重写（删掉的旧文档如 码农报告-round3~15 移入 docs/archive/ 子目录归档而非删除——历史证据不销毁）；Releases 说明只提 v9（交付）+samples（底包）；红线（个人研究不分发、APK 不入 git）保留。
- **docs/task-context.md 收口**：Current Status 更新为「v9 交付+真机验收通过+仓库维护态」，追加本清理指令执行记录占位。
- 提交 push（一条 commit 即可，message: `chore: 仓库收尾——移出 diff 归档/删搁置线 workflow/README 终态重写（东哥 9-12 指令）`）。

## 3. 平台清理（老马执行，码农勿动）

- Release 删除 16 个：v3-legacy v4 v6 v6b v6c v18 v22 v23 v24 v25 b3-shell v28 v33 v34 v35 v7 v8（保留 v9 + samples）
  - 注：v25 可能只有 tag 无 Release，按 API 实际列表为准
- Actions runs 删除：全部非现役线（fanqie-adfree-build / fanqie-v6-lspatch / fanqie-b3-shell 约 50 条）+ crack-diff/crack-patch 的历史失败 run（保留成功 run 做审计锚点）
- 老马完成后在本文件末尾追加执行记录（时间+API 响应计数）。

## 4. 验收

- `git ls-files | wc -l` < 100（docs md + scripts 现役 + workflows 2 条）
- 仓库 GitHub size 从 381MB 降到 <5MB（git gc 后）
- Releases 页只剩 v9 + samples
- Actions 列表只剩现役两条线
- README 新读者 3 分钟能理解项目现状与再生路径
- 全程不动：samples 资产、v9 资产、所有 tag、现役 workflow 触发链


---

## §3 执行记录（老马，2026-09-12 13:53 API 核验）

- Release：18→2。删 16 个（HTTP 204×16），读回验证=[v9(4 assets), samples(4 assets)] ✅
- Actions runs：64→5。删 59，读回验证=现役线成功 run 全保留：crack-patch×3（34671358636/34669310347/34610404428）+ crack-diff×2（34595927070/34593978471）✅
- tag 全部未动（19 个，含 samples/b3-shell 归档锚点）
