# 修复蓝图-破解版改造（round16b）v0.1

> 2026-09-11 东哥拍板：**卡密体系去掉**。D 类（com/rN 卡密 UI + arm/model 卡密 SDK）整体转去除候选。
> 上游：round16a-分类清单.md（老马审读）；round16a CI diff 已核实（460 changed + 1249 added）。
> 本轮原则：**只动已实锤点位，1082 混淆类本轮不动**（第二批再扫），单轮改动最小化，每步带证据。

## 第一步：依赖分析（patch 前必做，产出写 docs/round16b-依赖分析.md）

1. **com/a/a 设备标识链消费方反查**：getID/Randomid/Android_id/Reflect 的全部调用点（含 native 侧 sgcore0/libseccore 的注册方法）。判定：消费方是否只有卡密绑定（rN 显示设备码）+ 上报？
2. **sgcore0/libseccore 依赖分析**：libseccore.so 注册的 native 方法被哪些 Java 类调用？libsrtool.so 是否同一链路？判定：卡密验证是否走 native、so 是否还有卡密之外的消费方。
3. **MinePolarisBenefitRepository 语义确认**：方法体整体替换后的直接 return 返回值是什么（常量？对象？）——判定 VIP 权益是「无条件解锁」还是「依赖卡密激活态」。这决定第二步 VIP 策略。

## 第二步：patch 点位（按第一步证据执行条件规则）

| 点位 | 处理 | 条件 |
|---|---|---|
| com/rN 卡密激活 UI（9713 行弹窗类） | 入口 NOP：所有启动/跳转到 rN 的调用点移除（类文件保留防空引用） | 无条件（东哥拍板去除） |
| arm/model.* 卡密 SDK model 层（~10 类） | 同上：调用点 NOP，类保留 | 无条件 |
| MuteApplication.onCreate 第一行 com/a/a.init() | NOP 该调用 | 若第一步确认消费方仅卡密链 |
| com/a/a 设备标识全家 | 类保留不删（防反射/防 native 引用崩溃），仅断 Java 侧调用 | 同上 |
| sgcore0 + libseccore.so | 若卡密验证走 native 且无其他消费方：SafeLoader.registerNativesForClass 调用 NOP（so 文件保留在 apk 内不加载即可，不用删 so——减小 diff 面积） | 按第一步证据 |
| MinePolarisBenefitRepository | 保持 direct-return 现状；若 VIP 依赖卡密激活态：把卡密状态查询点 patch 为恒「已激活」（返回值伪造已激活），保证去卡密后 VIP 不断 | 按第一步证据 |
| AbsActivity/Ll83 桥 | 不动（已实锤=官方 AbsActivityCallback 桥，B 类） | 无条件 |
| 1082 混淆类 | 本轮不动 | 无条件 |

**VIP 兜底**：卡密去除后所有 VIP/权益判定点若仍有激活校验，统一 patch 为已激活态——东哥自用，卡密体系整体失效但不留崩溃点。

## 第三步：构建与交付

- CI 构建（fanqie-builder 仓，tag 递增规则不变），产物 + sha256 + 码农报告-round16b.md（每个 patch 点位的 diff 摘要 + 依赖分析结论）
- 老马 CT107/ACE5 装机三测：启动无崩溃 → 书城正常 → 卡密 UI 不再出现 → VIP 权益正常 → logcat 无 FATAL/VerifyError
- loop 判读沿用 loop-rules.json（J1: CI success + release + no-crash）

## 风险与回退

- native 链分析不确定时**保守处理**：只断 Java 调用点不删 so/register（宁可后门残骸不可崩溃）
-任一点位 patch 后 VerifyError → 该点位回退为「仅 NOP 调用点」，报告标注
- 预算：同靶心 ≤3 轮（loop-rules 拍板值）
